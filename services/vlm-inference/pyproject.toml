[project]
name = "vlm-inference"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "aiokafka",
    "openai",
    "langchain",
    "python-dotenv",
    "pydantic"
]
